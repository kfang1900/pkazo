import AppTemplate from 'AppTemplate';

const CartPage = () => {
    
    return (
        <AppTemplate>
            <h1>
                CartPage
            </h1>
        </AppTemplate>
    );
}

export default CartPage;
