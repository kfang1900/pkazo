import AppTemplate from 'AppTemplate';

const SellPage = () => {
    
    return (
        <AppTemplate>
            <h1>
                SellPage
            </h1>
        </AppTemplate>
    );
}

export default SellPage;
