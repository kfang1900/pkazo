import AppTemplate from 'AppTemplate';

const HomePage = () => {
    
    return (
        <AppTemplate>
            <h1>
                HomePage
            </h1>
        </AppTemplate>
    );
}

export default HomePage;
